{
  "name": "olexa-content-repository",
  "version": "0.1.0",
  "private": true,
  "description": "Searchable Olexa YouTube archive for GitHub Pages",
  "scripts": {
    "dev": "node scripts/dev-server.js",
    "sync": "node scripts/sync-youtube.js",
    "catalog": "node scripts/build-catalog.js",
    "check": "node --check js/common.js && node --check js/app.js && node --check js/games.js && node --check js/game.js && node --check js/community.js && node --check scripts/lib/game-catalog.js && node --check scripts/build-catalog.js && node --check scripts/validate-data.js && node --check scripts/sync-youtube.js && node --check scripts/dev-server.js",
    "test": "node --test",
    "validate": "node scripts/validate-data.js",
    "verify": "npm run check && npm test && npm run validate"
  },
  "engines": { "node": ">=18" }
}
